{
  "name": "aiforchemists",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "screenshot": "node scripts/screenshot-upload.mjs",
    "generate": "node scripts/generate-pages.mjs",
    "update": "npm run screenshot && npm run generate",
    "install-browsers": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "playwright": "^1.42.1",
    "playwright-core": "^1.42.1",
    "cloudinary": "^1.37.3",
    "fs-extra": "^11.2.0"
  }
}
